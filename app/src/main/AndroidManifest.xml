<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jchip.country.city">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".FallCountryActivity" android:screenOrientation="sensor" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FallWidgetFlagSetting"
            android:icon="@drawable/widget_icon"
            android:roundIcon="@drawable/widget_icon"
            android:theme="@style/widget_setting">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FallWidgetRatioSetting"
            android:icon="@drawable/widget_icon"
            android:roundIcon="@drawable/widget_icon"
            android:theme="@style/widget_setting">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".FallWidgetProvider"
            android:icon="@drawable/widget_icon">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fall_widget_info" />
        </receiver>
        <receiver
            android:name=".FallWidgetFlagProvider"
            android:label="@string/widget_flag">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fall_widget_flag" />
        </receiver>
        <receiver
            android:name=".FallWidgetRatioProvider"
            android:label="@string/widget_ratio">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fall_widget_ratio" />
        </receiver>
    </application>
</manifest>